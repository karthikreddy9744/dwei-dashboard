name: dwei

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip

  - pip:
      - streamlit
      - pandas
      - numpy
      - plotly

      - geopandas
      - fiona
      - pyproj
      - shapely

      - pyarrow
      - openpyxl

      - scikit-learn
      - scipy
      - joblib

      - xgboost
      - shap
      - numba

      - requests
      - tqdm